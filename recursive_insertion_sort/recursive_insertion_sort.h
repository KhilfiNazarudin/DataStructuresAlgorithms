#ifndef RECURSIVE_INSERTION_SORT_H
#define RECURSIVE_INSERTION_SORT_H

#include <cstddef>
#include <vector>

void recursiveInsertionSort(std::vector<int> &ar, std::size_t n);

#endif  // RECURSIVE_INSERTION_SORT_H
